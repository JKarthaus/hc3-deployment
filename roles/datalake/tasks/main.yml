
---


# This playbook installs Datalake Tools as Docker Service
# executes at ssh login

- name: create influxdb base dir
  file:
    path: /opt/datalake
    state: directory
    owner: pi
    group: root
    mode: 0775
  become: yes


- name: Copy docker-compose file
  copy:
    src: "templates/docker-compose.yml"
    dest: "/opt/datalake/docker-compose.yml"

- name: Pull docker images
  shell: "docker-compose -f /opt/datalake/docker-compose.yml pull"
  become: yes

- name: Create containers
  shell: "docker-compose -f /opt/datalake/docker-compose.yml up -d"
  become: yes

